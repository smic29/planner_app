<% if user_signed_in? %>
<div class="container">
    <%= yield %>
</div>

<% else %>
<div class="container-fluid home-main-container d-flex justify-content-center flex-column align-items-center">
    <div class="">
        <h1 class="">Welcome to Planner</h1>
    </div>
    <div class="d-flex justify-content-evenly w-25">        
        <%= link_to "Log In", new_user_session_path, class:"btn btn-outline-primary" %>
        <%= link_to "Sign Up", new_user_registration_path, class:"btn btn-outline-success" %>
    </div>
</div>

<% end %>
